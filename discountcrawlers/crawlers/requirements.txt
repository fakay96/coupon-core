# Core crawling stack
scrapy>=2.11
itemadapter>=0.8

# JavaScript‑rendering back‑ends
scrapy-splash>=0.9          # for Splash‑driven spiders
scrapy-playwright>=0.0.30   # for Playwright‑based spiders

# LLM / RAG utilities
httpx>=0.27                 # async HTTP client for Gemini calls
beautifulsoup4>=4.12        # HTML trimming (Gemini context)
lxml>=5.2                   # parser backend for BS4

# (Twisted comes with Scrapy; no separate pin needed.)
